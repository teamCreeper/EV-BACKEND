server:
  port: 8080

spring:
  application:
    name: EV
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/EVProject?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: tlsdmschd55
  thymeleaf:
    cache: false

  jpa:
    database: mysql
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true